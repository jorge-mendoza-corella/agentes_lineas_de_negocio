services:
  dashboard:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - apps/dashboard/.env.local
    ports:
      - "3001:3000"
    volumes:
      - /home/srvsozu/.ssh/id_rsa:/app/.ssh/agent_key:ro
    restart: unless-stopped
